# Serving-only dependencies for the deployed API and dashboard.
# The API only reads precomputed results, so the training stack (torch,
# xgboost, lightgbm, shap, mlflow, optuna, imbalanced-learn) is left out to
# fit a 512 MB free instance. Embeddings use chromadb's ONNX MiniLM instead
# of sentence-transformers (same model, no PyTorch).

# --- API ---
fastapi
uvicorn[standard]
pydantic

# --- data access ---
pandas
numpy
sqlalchemy
joblib
pyyaml
python-dotenv

# --- retrieval + generation ---
chromadb
onnxruntime          # ONNX MiniLM embeddings, no torch
langchain-groq

# --- dashboard ---
streamlit
plotly
requests

# --- needed only to unpickle the calibrator and read the preprocessor bundle ---
scikit-learn
